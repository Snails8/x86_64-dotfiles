_5cd‍.x([["indentStartTokens",()=>indentStartTokens],["indentEndTokens",()=>indentEndTokens],["indentElseTokens",()=>indentElseTokens],["optionalStartWithoutEndTokens",()=>optionalStartWithoutEndTokens],["tokenForIndentStartOrElseTokens",()=>tokenForIndentStartOrElseTokens],["indentStartOrElseTokens",()=>indentStartOrElseTokens],["indentStartAndEndTokens",()=>indentStartAndEndTokens],["phpKeywordStartTokens",()=>phpKeywordStartTokens],["phpKeywordEndTokens",()=>phpKeywordEndTokens],["inlineFunctionTokens",()=>inlineFunctionTokens],["hasStartAndEndToken",()=>hasStartAndEndToken]]);const _ = require('lodash');

       const indentStartTokens = [
  '@alert',
  '@auth',
  '@canany',
  '@cannot',
  '@can',
  '@role',
  '@hasrole',
  '@hasanyrole',
  '@hasallroles',
  '@unlessrole',
  '@hasexactroles',
  '@component',
  '@empty',
  '@env',
  '@foreach',
  '@for',
  '@forelse',
  '@guest',
  '@hassection',
  '@if',
  '@isset',
  '@permission',
  '@permissions',
  '@php',
  '@push',
  '@section',
  '@slot',
  '@switch',
  '@unless',
  '@verbatim',
  '@while',
  '@production',
  '@prepend',
  '@once',
  '@error',
];

       const indentEndTokens = [
  '@endalert',
  '@endauth',
  '@endcanany',
  '@endcannot',
  '@endcan',
  '@endrole',
  '@endhasrole',
  '@endhasanyrole',
  '@endhasallroles',
  '@endunlessrole',
  '@endhasexactroles',
  '@endcomponent',
  '@endempty',
  '@endenv',
  '@endfor',
  '@endforeach',
  '@endforelse',
  '@endguest',
  '@endif',
  '@endisset',
  '@endphp',
  '@endpush',
  '@endsection',
  '@endslot',
  '@endswitch',
  '@endunless',
  '@endverbatim',
  '@endwhile',
  '@show',
  '@stop',
  '@endproduction',
  '@endprepend',
  '@endonce',
  '@enderror',
  '@append',
  '@overwrite',
];

       const indentElseTokens = [
  '@elseenv',
  '@elseif',
  '@elsecanany',
  '@elsecannot',
  '@elsecan',
  '@else',
];

// Directives which do not need an end token if a parameter is present
       const optionalStartWithoutEndTokens = {
  '@section': 2,
  '@push': 2,
  '@prepend': 2,
};

       const tokenForIndentStartOrElseTokens = ['@forelse'];

       const indentStartOrElseTokens = ['@empty'];

       const indentStartAndEndTokens = ['@default'];

       const phpKeywordStartTokens = [
  '@forelse',
  '@if',
  '@for',
  '@foreach',
  '@while',
  '@sectionmissing',
  '@case',
];

       const phpKeywordEndTokens = [
  '@endforelse',
  '@endif',
  '@endfor',
  '@endforeach',
  '@endwhile',
  '@break',
];

       const inlineFunctionTokens = ['@json'];

       function hasStartAndEndToken(tokenizeLineResult, originalLine) {
  return (
    _.filter(tokenizeLineResult.tokens, (tokenStruct) => {
      const token = originalLine
        .substring(tokenStruct.startIndex, tokenStruct.endIndex)
        .trim();

      return (
        _.includes(indentStartTokens, token) ||
        _.includes(indentEndTokens, token)
      );
    }).length >= 2
  );
}
